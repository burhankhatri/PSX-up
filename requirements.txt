# Vercel API Dependencies
# These run on Vercel serverless functions

# E2B SDK for sandbox execution
e2b-code-interpreter>=0.0.11

# Web framework
fastapi>=0.109.0
python-dotenv>=1.0.0

# HTTP client
httpx>=0.26.0

# Anthropic for sentiment analysis (primary, runs on Vercel, not in E2B)
anthropic>=0.39.0

# Groq for sentiment analysis (fallback, runs on Vercel, not in E2B)
groq>=0.4.0

# JSON handling
pydantic>=2.5.0
